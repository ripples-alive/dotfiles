# ripples's dotfiles

## setup

```bash
apt install git tmux vim zsh
git clone --depth 1 https://github.com/ripples-alive/dotfiles.git .dotfiles
.dotfiles/install
```

## update

```bash
sync-dotfiles
```
